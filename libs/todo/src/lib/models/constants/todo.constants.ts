export const CATEGORY = {
  completed: ['in progress', 'completed'],
  priority: ['HIGH', 'LOW', 'MEDIUM'],
};

export const SORT_CATEGORY = ['title', 'description', 'id', 'createAt'];

export const TODO_FILTER_KEYS = ['priority', 'completed'];

/**
 * The key for this feature's state in the global state
 */
export const FEATURE_KEY = 'todo';
